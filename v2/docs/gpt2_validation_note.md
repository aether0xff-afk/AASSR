# GPT-2 validation checklist

The implementation is not ready to merge until all of the following pass:

- full v2 unit-test suite
- GPT2_REWARD smoke run
- GPT2_ACADEMY_MODEL smoke run
- matched baseline output generation
- `academy_report.json` contains creativity guardrail fields

Large paper-facing runs must only begin after these checks pass.
